{
  "name": "finalV",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix client\" \"npm run dev --prefix files\"",
    "dev:client": "npm run dev --prefix client",
    "dev:server": "npm run dev --prefix files",
    "build": "npm run build --prefix client",
    "start": "node files/server.js",
    "postinstall": "npm install --prefix client && npm install --prefix files && npm run build --prefix client"
  },
  "dependencies": {
    "concurrently": "^9.2.1"
  }
}
